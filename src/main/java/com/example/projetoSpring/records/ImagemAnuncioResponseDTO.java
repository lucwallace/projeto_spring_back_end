package com.example.projetoSpring.records;

public record ImagemAnuncioResponseDTO(Integer id,
                                       String url,
                                       String publicId,
                                       String pasta) {
}
